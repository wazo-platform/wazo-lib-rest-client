#!/usr/bin/make -f
# -*- makefile -*-

export PYBUILD_NAME=xivo_lib_rest_client
export PYBUILD_DESTDIR=debian/xivo-lib-rest-client/
export PYBUILD_DISABLE=test

%:
	dh $@ --with python2,python3 --buildsystem=pybuild
